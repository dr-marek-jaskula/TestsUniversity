version: '3.9'

services:

  db:
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=DataBase
      - POSTGRES_DB=TestDatabase
    ports:
      - '5432:5432'
